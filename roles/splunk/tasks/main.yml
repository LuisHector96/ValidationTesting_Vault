---
# tasks file for roles/splunk

- name: Populate service facts
  service_facts:

- debug:
    var: ansible_facts.services['splunk.service']

- name: Splunk Forwarder Status
  debug:
    msg: Splunk Forwarder Up and Running
  when: ansible_facts.services['splunk.service'].state == 'running'